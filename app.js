window.addEventListener('DOMContentLoaded', (event) => {
    event.preventDefault();
            //Select Button
            let button = document.querySelector("#quoteButton")
            //Event Listener to "button"
            button.addEventListener("click", (event2) => {
                
                //prevent browser default action
                event2.preventDefault();
                //def the containers of the info we target
            let quote = document.querySelector("#quoteContent p");
            let quoteGenius = document.querySelector("#quoteAuthor");              
            });
        });